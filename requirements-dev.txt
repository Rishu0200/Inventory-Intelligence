-r requirements-train.txt

# Synthetic data generation (one-time/local only)
faker>=26.0.0
reportlab>=4.2.0
sdv>=1.13.0

# PDF ingestion (one-time/local only, unless you later move ingestion into CI too)
pymupdf>=1.24.0
datasketch>=1.6.5
langchain-text-splitters

# Testing
pytest>=8.3.0
httpx>=0.27.0
pytest-asyncio>=0.24.0

ipykernel